# ============================================================================
# 仓库根 .gitignore
#
# 仓库根 = /Users/chenwei/Desktop/deepseek/
#   ├── AGENTS.md / SKILLS.md        代理约定与 skill 索引（应提交）
#   ├── .agents/skills/              项目级 skill（应提交）
#   ├── docs/                        设计与接口文档（应提交）
#   └── ai-efficiency-console/       应用代码（应提交）
#
# 原则：只忽略**可再生成的产物**与**本机/个人配置**；凡是「不看代码就写不出来」
# 的东西（口径说明、skill、设计文档、mock 数据集的生成器）一律入库。
# 注意 `.gitignore` 里的 `.` 开头目录默认**不会**被自身规则忽略，
# 因此 `.agents/` 需要显式例外（见文末）。
# ============================================================================

# ---------------------------------------------------------------------------
# 依赖与包管理
# ---------------------------------------------------------------------------
node_modules/
.pnpm-store/
.yarn/

# npm 缓存：本仓库把缓存放在工作区内（因为默认 ~/.npm 可能不可写）
# 见 AGENTS.md §2 的 `npm install --cache ../.npm-cache`
.npm-cache/
.npm/
npm-debug.log*
yarn-error.log*

# Python 虚拟环境与缓存
.venv/
venv/
env/
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---------------------------------------------------------------------------
# 构建产物（可由源码 + 数据重新生成）
# ---------------------------------------------------------------------------
dist/
build/
*.tsbuildinfo
vite.config.ts.timestamp-*

# ---------------------------------------------------------------------------
# 本项目的生成物与临时产物
# ---------------------------------------------------------------------------
# 页面自检截图：由 `npm run verify` 生成，每次运行都会覆盖
ai-efficiency-console/frontend/verification/
# 自检与调试的临时日志
*.log
/tmp/

# ---------------------------------------------------------------------------
# 数据
# ---------------------------------------------------------------------------
# data/ 只放**生成物**：数据集由 gen_mock.py 按「运行当天」为窗口终点生成，
# 入库当天就会过期，且 3.5MB 的 blob 进了历史就删不掉。所以目录入库、产物忽略：
# 保留 .gitkeep 占位；clone 后先跑 `python3 backend/app/gen_mock.py`。
ai-efficiency-console/data/*
!ai-efficiency-console/data/.gitkeep
#
# 若某次需要留档（例如给业务方归档某期的样例数据），放这里，目录整体入库：
!ai-efficiency-console/data/samples/
!ai-efficiency-console/data/samples/**
#
# 真实企业数据/导出文件绝不入库
*.sqlite
*.sqlite3
*.db
*.csv
!ai-efficiency-console/data/samples/*.csv
secrets.json

# ---------------------------------------------------------------------------
# 环境变量与凭据（企业 OpenAPI token 只能放在服务端，禁止入库）
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
credentials.*
.credentials*

# ---------------------------------------------------------------------------
# 编辑器与操作系统
# ---------------------------------------------------------------------------
.DS_Store
._*
.Spotlight-V100
.Trashes
.idea/
*.iml
.vscode/*
!.vscode/extensions.json
*.swp
*.swo
*~

# ---------------------------------------------------------------------------
# 需要显式保留的隐藏目录
# ---------------------------------------------------------------------------
# 说明：Git 默认不会因为「以点开头」而忽略目录，`.agents/` 本来就会被跟踪。
# 这里显式声明是为了防止后续有人加了 `.*` 这类宽泛规则而误伤项目级 skill。
!.agents/
!.agents/**
!.github/
